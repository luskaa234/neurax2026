# NEURAX Web App

Painel principal da plataforma NEURAX.

Fluxo alvo:
1. Ideia (chat)
2. Arquitetura (template + módulos)
3. Build (logs e progresso)
4. IDE (Monaco + preview)

A UI atual do painel está na raiz (`src/`) e será gradualmente migrada para `apps/web`.
